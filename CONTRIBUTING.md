# Contributing

Read the [contributor guide](clientops-tracker/CONTRIBUTING.md).
Application commands run inside `clientops-tracker/`, not the repository root.
Workflows and GitHub issue templates live in the root `.github/` directory.
Use the dedicated test database, never development or production data.
